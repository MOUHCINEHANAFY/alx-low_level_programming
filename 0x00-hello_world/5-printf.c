#include <stdio.h>

/**
 * main - print with printf "with proper grammar.."
 * Return: 0 if succesful program
 * */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
