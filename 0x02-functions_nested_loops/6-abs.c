#include "main.h"

/**
 * _abs - return abslute value of int
 * @n: input to be calculated
 * Return: absolut of int number
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
