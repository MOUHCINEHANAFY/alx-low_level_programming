#include <stdio.h>

/**
 * main - Print programe name
 * @argc: argument number of the programme
 * @argv: array pointer to arguments
 * Return: return 0 if sucess
 */

int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
